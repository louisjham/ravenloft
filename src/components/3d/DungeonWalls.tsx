import React from 'react';

/**
 * DungeonWalls component.
 * Static enclosing walls have been removed to prevent clipping as the dungeon layout expands.
 * Edge walls are now dynamically rendered by individual tiles on closed connections.
 */
export const DungeonWalls: React.FC = () => {
  return null;
};
